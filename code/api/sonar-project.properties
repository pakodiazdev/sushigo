sonar.projectKey=pakodiazdev_sushigo-api
sonar.organization=pakodiazdev-1

# Source & test paths (relative to code/api/)
sonar.sources=app,routes,database
sonar.tests=tests

# Exclusions
sonar.exclusions=vendor/**,storage/**,bootstrap/**,public/**
sonar.coverage.exclusions=database/**,routes/**,tests/**

# Migrations are point-in-time schema snapshots — retroactively deduplicating
# them (e.g. create_cash_expenses_table / create_cash_adjustments_table sharing
# column-definition blocks) risks breaking chronological schema application
# for no behavioral benefit. See issue #282.
sonar.cpd.exclusions=database/migrations/**

# PHP coverage — generated by: php artisan test --coverage-clover=coverage.xml
sonar.php.coverage.reportPaths=coverage.xml
